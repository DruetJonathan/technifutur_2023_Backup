import { FormatChronoPipe } from './format-chrono.pipe';

describe('FormatChronoPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatChronoPipe();
    expect(pipe).toBeTruthy();
  });
});
